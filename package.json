{
  "name": "adriano-financas",
  "version": "1.0.0",
  "description": "Landing page de captura de leads — @adrianosantos_pf",
  "scripts": {
    "dev": "npx serve public",
    "build": "echo 'Static site — no build needed'",
    "start": "npx serve public"
  },
  "engines": {
    "node": ">=18"
  }
}